export const errorHandlerApiRoute = (error: unknown) => {
  console.error("errorHandlerApiRoute", error);
};

export const errorHandlerApi = (error: unknown) => {
  console.error("errorHandlerApi", error);
};

export const errorHandlerApp = (error: unknown) => {
  console.error("errorHandlerApp", error);
};

export const errorHandlerUi = (error: unknown) => {
  console.error("errorHandlerUi", error);
};
